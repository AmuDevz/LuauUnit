local init = {}

local Fact = require(script.Fact)
local Assert = require(script.Assert)
local Runner = require(script.Runner)

init.Fact = Fact
init.Assert = Assert
init.Runner = Runner

return init
